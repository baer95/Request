<?php

        // $GLOBALS     // Brauch ich nicht, da es nur eine Sammlung von globalen Variablen ist
        // $_REQUEST    // Brauch ich nicht, da get, post und cookie enthalten sind, die extra durchlaufen werden sollen
            $_GET;
            $_POST;
            $_COOKIE;
        $_SERVER;   // ?
        $_FILES;        // muss ich schätz i mal schon durchlaufen
        // $_SESSION    // Brauch ich nicht, da der Anwender keine möglichkeiten hat, etwas in die Session-Variable zu schreiben.
        // $_ENV;       // Brauch ich nicht, da ENVIRONMENT-Variablen nur in der Apache-Konfiguration festgelegt werden.
